---
phase: 02-config-team-greenfield-planning
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/app/services/github_client.py
  - backend/app/graph/nodes/read_docs_greenfield.py
  - backend/app/graph/nodes/ingest_brownfield_stub.py
  - backend/app/graph/build.py
  - backend/app/graph/state.py
  - backend/tests/test_github_client.py
  - backend/tests/test_build_graph.py
  - backend/requirements.txt
autonomous: true
requirements: [REPO-01, REPO-02]

must_haves:
  truths:
    - "When REPO_MODE=greenfield (or unset), the graph reads the configured repo's README + docs/**/*.md and carries that text forward for plan generation"
    - "When REPO_MODE=brownfield, the graph takes a distinct, honest placeholder path that clearly states brownfield planning arrives in Phase 5 — it never silently falls back to greenfield or crashes"
    - "A greenfield repo with no README and no docs/**/*.md files blocks the run with a clear 'No project docs found' message rather than generating a plan from nothing"
    - "A run whose smoke-test failed (Plan 01) never reaches doc-fetching or plan generation"
  artifacts:
    - path: "backend/app/services/github_client.py"
      provides: "fetch_greenfield_docs() — tree enumeration + targeted content fetch, capped size"
      contains: "def fetch_greenfield_docs"
    - path: "backend/app/graph/nodes/read_docs_greenfield.py"
      provides: "Real greenfield node calling github_client, setting docs_text or blocked_reason"
      contains: "fetch_greenfield_docs"
    - path: "backend/app/graph/nodes/ingest_brownfield_stub.py"
      provides: "Guarded brownfield placeholder node"
      contains: "blocked_reason"
    - path: "backend/app/graph/build.py"
      provides: "Conditional edge after ingest_config routing on smoke_test_passed then repo_mode"
      contains: "add_conditional_edges"
  key_links:
    - from: "backend/app/graph/build.py"
      to: "backend/app/graph/nodes/read_docs_greenfield.py"
      via: "add_conditional_edges(\"ingest_config\", route_after_config, ...)"
      pattern: "add_conditional_edges"
    - from: "backend/app/graph/nodes/read_docs_greenfield.py"
      to: "backend/app/services/github_client.py"
      via: "await github_client.fetch_greenfield_docs(...)"
      pattern: "github_client\\.fetch_greenfield_docs"
---

<objective>
Replace the straight-line `ingest_config -> stub_plan` edge with a real
conditional branch: after config/smoke-test, route on `smoke_test_passed`
first (block on failure) and then on `repo_mode` (REPO-01) — greenfield reads
the repo's docs for real (REPO-02) via `PyGithub`'s tree-enumeration +
targeted-fetch pattern, brownfield takes a guarded, honest placeholder that
converges into the same downstream node without doing real work (D-09).

Purpose: This is the branch-half of ORCH-01 that Phase 1 explicitly deferred
(01-CONTEXT.md D-01/D-02) and the exact mechanism REPO-01/REPO-02 require. It
must sit correctly between Plan 01's smoke-test gate and Plan 04's real plan
generation, since both of those plans read/write state this plan's routing
function inspects.
Output: `github_client.fetch_greenfield_docs()`, two new graph nodes
(`read_docs_greenfield`, `ingest_brownfield_stub`), and `build.py`'s
conditional-edge wiring — `stub_plan` remains the terminal node for this plan
(Plan 04 replaces it with real generation in the next wave).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-config-team-greenfield-planning/02-CONTEXT.md
@.planning/phases/02-config-team-greenfield-planning/02-RESEARCH.md
@.planning/phases/02-config-team-greenfield-planning/02-01-SUMMARY.md

<interfaces>
<!-- RunState fields Plan 01 (02-01) added — this plan reads/extends them -->
From backend/app/graph/state.py (after Plan 01, current):
```
class RunState(TypedDict, total=False):
    run_id: str
    lead_email: str
    plan: Plan
    approved: bool
    pushed: bool
    push_report: PushReport
    repo_mode: str            # "greenfield" | "brownfield", defaulted in ingest_config
    smoke_test_passed: bool
    smoke_test: dict
```
This plan adds: `docs_text: str | None`, `blocked_reason: str | None`.

From backend/app/graph/build.py (current, Phase 1 straight-line spine, to be
modified — NOT rewritten from scratch):
```
def build_graph() -> StateGraph:
    builder = StateGraph(RunState)
    builder.add_node("ingest_config", ingest_config)
    builder.add_node("stub_plan", stub_plan)
    builder.add_node("human_review", human_review)
    builder.add_node("push_to_ado", push_to_ado)
    builder.add_edge(START, "ingest_config")
    builder.add_edge("ingest_config", "stub_plan")
    builder.add_edge("stub_plan", "human_review")
    builder.add_edge("human_review", "push_to_ado")
    builder.add_edge("push_to_ado", END)
    return builder
```

From backend/app/graph/nodes/ingest_config.py (after Plan 01): returns
`lead_email`, `repo_mode`, `smoke_test_passed`, `smoke_test` — this plan's
routing function reads `state["smoke_test_passed"]` and `state["repo_mode"]`.

PyGithub reference signatures (pygithub.readthedocs.io):
```
Github(token: str)
Github.get_repo(full_name_or_id: str) -> Repository
Repository.default_branch -> str
Repository.get_git_tree(sha: str, recursive: bool = False) -> GitTree
GitTree.tree -> list[GitTreeElement]  # each has .path, .type ("blob"|"tree")
Repository.get_contents(path: str) -> ContentFile  # .content (base64), .decoded_content (bytes)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: github_client doc-fetch service with tree enumeration + size cap</name>
  <files>backend/app/services/github_client.py, backend/tests/test_github_client.py, backend/requirements.txt</files>
  <read_first>.planning/phases/02-config-team-greenfield-planning/02-RESEARCH.md (Pattern 2, Pitfall 5), backend/app/services/ado_client.py (module style to mirror: module-level constants, functions read os.environ fresh, never at import time)</read_first>
  <behavior>
  - Test 1: Given a fake tree fixture (list of path strings mimicking
    `GitTree.tree` elements) containing `README.md`, `docs/setup.md`,
    `docs/nested/deep.md`, and unrelated paths (`src/main.py`,
    `package-lock.json`), the doc-matching filter selects exactly the three
    doc paths (case-insensitive `README*` match at any path depth is NOT
    required — only match `README*` at the repo root per D-11's plain
    "README" wording — and any path where
    `path.lower().startswith("docs/") and path.lower().endswith(".md")`, per
    Pitfall 5's explicit guidance to avoid `fnmatch`'s `**`).
  - Test 2: Given matched paths and mocked `get_contents()` return values,
    `fetch_greenfield_docs` concatenates their decoded text with a
    `--- {path} ---` header per file and stops once `max_chars` is reached,
    truncating the final chunk rather than exceeding the cap.
  - Test 3: Given a tree fixture with no matching paths, `fetch_greenfield_docs`
    returns `None` (D-12's signal for "no usable docs found").
  - Test 4: `fetch_greenfield_docs` is called with an explicit `max_chars`
    parameter (default 60_000) — verify a small `max_chars` (e.g. 50) applied
    to two long file fixtures actually truncates and stops early rather than
    fetching every matched file regardless of the cap.
  </behavior>
  <action>
  Add `PyGithub==2.9.1` to `backend/requirements.txt` (confirm this is still
  the current top version via `pip index versions PyGithub` before
  committing to it — RESEARCH.md verified this same version on 2026-07-10, one
  day prior).

  Create `backend/app/services/github_client.py`. Implement
  `fetch_greenfield_docs(token: str, owner_repo: str, max_chars: int =
  60_000) -> str | None` following RESEARCH.md's Pattern 2 code example
  closely, with the Pitfall-5 fix applied (do NOT use
  `fnmatch.fnmatch(path, "docs/**/*.md")` — use the explicit
  `startswith("docs/")`/`endswith(".md")` check instead). Structure the
  function so the tree-fetch (`Github(token).get_repo(owner_repo)`,
  `repo.get_git_tree(repo.default_branch, recursive=True)`), the path-matching
  (pure function, e.g. `_match_doc_paths(tree_paths: list[str]) ->
  list[str]`, independently testable without network calls), and the
  content-fetch-and-concatenate loop are separated so Task tests can exercise
  `_match_doc_paths` and the concatenation logic against fixtures without
  needing a real `Github` client — inject the `Github` instance or accept a
  pre-fetched `tree_paths: list[str]` + a `get_contents_fn` callable as
  parameters if that makes the function more testable (Claude's discretion on
  exact signature split, but the path-matching logic MUST be unit-testable in
  isolation per Test 1).

  README matching: match paths where the path has no `/` (repo root) and
  `path.lower().startswith("readme")` — do not match `docs/README.md` as the
  root README (that's covered by the `docs/` rule already if it ends in
  `.md`; if it's `docs/README` with no `.md` extension, it is correctly
  excluded per D-11's `docs/**/*.md` scope, which requires `.md`).
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_github_client.py -x</automated>
  </verify>
  <done>fetch_greenfield_docs correctly selects README + docs/**/*.md paths using explicit string matching (not fnmatch **), concatenates and caps at max_chars, and returns None when no docs match.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conditional edge wiring — smoke-test gate, repo_mode branch, brownfield placeholder</name>
  <files>backend/app/graph/nodes/read_docs_greenfield.py, backend/app/graph/nodes/ingest_brownfield_stub.py, backend/app/graph/build.py, backend/app/graph/state.py, backend/tests/test_build_graph.py</files>
  <read_first>backend/app/graph/build.py, backend/app/graph/state.py, backend/app/graph/nodes/ingest_config.py, backend/app/graph/nodes/stub_plan.py, .planning/phases/02-config-team-greenfield-planning/02-RESEARCH.md (Pattern 1, D-09 note)</read_first>
  <behavior>
  - Test 1: A routing function `route_after_config(state) -> str` returns
    `"blocked"` when `state.get("smoke_test_passed") is False`, regardless of
    `repo_mode`.
  - Test 2: `route_after_config` returns `"ingest_brownfield_stub"` when
    `smoke_test_passed` is True and `repo_mode == "brownfield"`.
  - Test 3: `route_after_config` returns `"read_docs_greenfield"` when
    `smoke_test_passed` is True and `repo_mode` is `"greenfield"` or unset/
    missing (default-to-greenfield safety net, mirrors ingest_config's own
    default).
  - Test 4: The compiled graph (via `build_graph().compile()` with an
    in-memory checkpointer for the test) actually reaches
    `ingest_brownfield_stub` for a brownfield state and `read_docs_greenfield`
    for a greenfield state — an integration-level check on top of the pure
    routing-function unit tests, using LangGraph's sync `.invoke()` with a
    stubbed `ingest_config` behavior via monkeypatching `ado_client.run_smoke_test`
    to avoid real network calls.
  </behavior>
  <action>
  In `backend/app/graph/state.py`, add two fields to `RunState`: `docs_text:
  str | None`, `blocked_reason: str | None`.

  Create `backend/app/graph/nodes/read_docs_greenfield.py`: `async def
  read_docs_greenfield(state: RunState) -> dict`. Reads `GITHUB_REPO` and
  `GITHUB_TOKEN` from `os.environ` (mirrors `ingest_config`'s "read fresh from
  env" pattern; do not thread these through `RunState` — they're config, not
  run data, per D-04). Calls `github_client.fetch_greenfield_docs(token,
  github_repo)`. If the result is `None`, return `{"docs_text": None,
  "blocked_reason": "No project docs found — add a README to plan"}` (D-12
  exact wording). Otherwise return `{"docs_text": result, "blocked_reason":
  None}`.

  Create `backend/app/graph/nodes/ingest_brownfield_stub.py`: `def
  ingest_brownfield_stub(state: RunState) -> dict`. Returns `{"docs_text":
  None, "blocked_reason": "Brownfield planning arrives in Phase 5 — this run
  cannot generate a plan yet."}` — a synchronous, zero-network, guarded
  placeholder per D-09. Never raises, never calls any brownfield ingestion
  logic (none exists yet).

  In `backend/app/graph/build.py`: add a module-level `def
  route_after_config(state: RunState) -> str` function (not a node) with the
  exact three-way logic from the Behavior section above. Add
  `"blocked"` as a valid routing target — since there is no dedicated
  "blocked" node yet, route `"blocked"` directly to `END` via the
  conditional-edges mapping (`{"blocked": END, "read_docs_greenfield":
  "read_docs_greenfield", "ingest_brownfield_stub": "ingest_brownfield_stub"}`).
  This means a failed-smoke-test run reaches `END` with `smoke_test_passed:
  False` already visible in state (Plan 01's `runs.py` status derivation
  already surfaces this via the `blocked_smoke_test_failed` status override —
  no further node processes it). Add the two new node registrations
  (`builder.add_node("read_docs_greenfield", read_docs_greenfield)`,
  `builder.add_node("ingest_brownfield_stub", ingest_brownfield_stub)`).
  Replace the existing `builder.add_edge("ingest_config", "stub_plan")` line
  with `builder.add_conditional_edges("ingest_config", route_after_config,
  {"blocked": END, "read_docs_greenfield": "read_docs_greenfield",
  "ingest_brownfield_stub": "ingest_brownfield_stub"})`. Add
  `builder.add_edge("read_docs_greenfield", "stub_plan")` and
  `builder.add_edge("ingest_brownfield_stub", "stub_plan")` (both converge
  into `stub_plan` for now — Plan 04 replaces `stub_plan` with the real
  `generate_plan` node in the next wave; this plan does not touch
  `stub_plan.py`'s body).

  Note: `stub_plan.py`'s current body ignores `docs_text`/`blocked_reason`
  entirely (it's Phase 1's hardcoded stub) — that is expected and correct for
  this plan's scope. Plan 04 is the one that makes `generate_plan` actually
  consume `docs_text` and short-circuit on `blocked_reason`, per RESEARCH.md's
  explicit note ("its returned state should set a flag... that generate_plan
  checks first and short-circuits on").
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_build_graph.py -x</automated>
  </verify>
  <done>route_after_config correctly routes blocked/greenfield/brownfield states; the compiled graph reaches the correct node for each repo_mode; a failed smoke-test short-circuits straight to END without reaching doc-fetch or plan generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GitHub repo content -> LLM prompt (Plan 04 consumes docs_text) | Untrusted repo doc content flows into a downstream LLM prompt; this plan only fetches/caps it, Plan 04 owns prompt construction |
| GITHUB_TOKEN -> GitHub REST API | Outbound call carrying the token; never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Malicious README/docs content attempting prompt injection against the downstream LLM | accept | Low severity for a local single-lead MVP with no multi-tenant exposure; docs_text is treated as data, not instructions, when Plan 04 builds its prompt (documented here as a constraint on Plan 04, not mitigated in this plan since no LLM call happens yet) |
| T-02-07 | Denial of Service | Unbounded doc concatenation from a very large docs/ tree | mitigate | `max_chars` cap (default 60_000) enforced in `fetch_greenfield_docs`, verified by Task 1 Test 4 |
| T-02-08 | Information Disclosure | GITHUB_TOKEN | mitigate | Read fresh from `os.environ` inside the node, never stored in `RunState` or logged |
</threat_model>

<verification>
- `cd backend && pytest tests/test_github_client.py tests/test_build_graph.py -x` passes
- `cd backend && pytest -x` (full suite) passes, confirming no regression to Plan 01's smoke-test tests
</verification>

<success_criteria>
- REPO-01 satisfied: the graph correctly branches on `repo_mode`, defaulting to greenfield, with brownfield as a guarded non-crashing placeholder
- REPO-02 satisfied: greenfield reads README + docs/**/*.md via `get_git_tree` + targeted `get_contents()`, no shallow clone
- A failed smoke-test (Plan 01) is a dead end at `END`, never reaching doc-fetch/plan-generation nodes
</success_criteria>

<output>
Create `.planning/phases/02-config-team-greenfield-planning/02-03-SUMMARY.md` when done
</output>
