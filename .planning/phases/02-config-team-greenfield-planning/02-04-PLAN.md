---
phase: 02-config-team-greenfield-planning
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - backend/app/models/skills.py
  - backend/app/models/plan.py
  - backend/app/services/llm.py
  - backend/app/graph/nodes/generate_plan.py
  - backend/app/graph/nodes/stub_plan.py
  - backend/app/graph/build.py
  - backend/tests/test_generate_plan.py
  - backend/requirements.txt
  - backend/.env.example
  - frontend/src/lib/runClient.ts
  - frontend/src/pages/RunPage.tsx
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04]

must_haves:
  truths:
    - "Approving a greenfield run with real docs produces a real LLM-generated plan of 2-5 epics with 2-6 tasks each, not a hardcoded stub"
    - "Every generated task carries a skill_tag from the fixed ~12-20 item taxonomy and an estimate_hours value"
    - "Malformed or taxonomy-violating LLM output triggers an automatic repair-retry (bounded attempts), and exhausting retries fails loudly with a clear error rather than surfacing broken plan data"
    - "suggested_assignee is empty (\"\") on every generated task — Phase 3 owns assignment, not this phase"
    - "A blocked run (failed smoke-test, no-docs greenfield, or brownfield placeholder) never invokes the LLM at all"
  artifacts:
    - path: "backend/app/models/skills.py"
      provides: "Fixed skill taxonomy constant, importable by both the plan-generation node and API models"
      contains: "SKILL_TAXONOMY"
    - path: "backend/app/services/llm.py"
      provides: "ChatOpenAI factory (NVIDIA NIM base_url) + generate_plan_with_repair validate/repair loop"
      contains: "def generate_plan_with_repair"
    - path: "backend/app/graph/nodes/generate_plan.py"
      provides: "Real plan-generation node replacing stub_plan, short-circuits on blocked_reason"
      contains: "blocked_reason"
    - path: "backend/app/graph/build.py"
      provides: "generate_plan wired in place of stub_plan"
      contains: "generate_plan"
  key_links:
    - from: "backend/app/graph/nodes/generate_plan.py"
      to: "backend/app/services/llm.py"
      via: "generate_plan_with_repair(llm, docs_text, SKILL_TAXONOMY)"
      pattern: "generate_plan_with_repair"
    - from: "backend/app/services/llm.py"
      to: "backend/app/models/plan.py"
      via: "with_structured_output(Plan, method=\"function_calling\", include_raw=True)"
      pattern: "with_structured_output"
    - from: "frontend/src/pages/RunPage.tsx"
      to: "frontend/src/lib/runClient.ts"
      via: "renders task.skill_tag from the Plan interface"
      pattern: "skill_tag"
---

<objective>
Replace `stub_plan` with a real, GLM-backed `generate_plan` node: constrain
task skill tags to a fixed taxonomy (PLAN-02), estimate every task in hours
(PLAN-03), generate a bounded 2-5 epic / 2-6 task plan (PLAN-01/D-13) via
structured tool-calling, and wrap the LLM call in a hand-rolled
validate-then-repair retry loop that fails loudly rather than emitting broken
data (PLAN-04) — since `OutputFixingParser` is removed from current
`langchain-core` and GLM-via-NIM has a documented malformed-tool-call-JSON bug
report this loop must defend against.

Purpose: This is the plan-generation heart of Phase 2's primary demo path —
the payoff of Plan 01's smoke-test gate and Plan 03's doc-fetch/branch work.
Output: `services/llm.py` (ChatOpenAI factory + repair loop),
`models/skills.py` (taxonomy), `graph/nodes/generate_plan.py` (replaces
`stub_plan` in the graph), and a frontend update so the lead can see
`skill_tag` on each rendered task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-config-team-greenfield-planning/02-CONTEXT.md
@.planning/phases/02-config-team-greenfield-planning/02-RESEARCH.md
@.planning/phases/02-config-team-greenfield-planning/02-03-SUMMARY.md
@.planning/research/STACK.md

<interfaces>
<!-- Current shared Plan schema — Task.skill_tag currently str | None, must
     become taxonomy-constrained per Pitfall 4 -->
From backend/app/models/plan.py (current):
```python
class Task(BaseModel):
    id: str
    title: str
    description: str
    suggested_assignee: str
    estimate_hours: float
    skill_tag: str | None = None  # Phase 1: unused/None; Phase 2+ populates
    depends_on: list[str] = []

class Epic(BaseModel):
    id: str
    title: str
    description: str
    tasks: list[Task]

class Plan(BaseModel):
    epics: list[Epic]
```

<!-- RunState fields this node reads (from Plan 01 + Plan 03) -->
From backend/app/graph/state.py (after Plan 03):
```python
class RunState(TypedDict, total=False):
    run_id: str
    lead_email: str
    plan: Plan
    approved: bool
    pushed: bool
    push_report: PushReport
    repo_mode: str
    smoke_test_passed: bool
    smoke_test: dict
    docs_text: str | None
    blocked_reason: str | None
```

<!-- STACK.md's exact GLM-via-NIM wiring (do not deviate) -->
From .planning/research/STACK.md "GLM via NVIDIA NIM — Exact Wiring":
```python
from langchain_openai import ChatOpenAI
llm = ChatOpenAI(
    model=os.environ["NVIDIA_CHAT_MODEL"],
    api_key=os.environ["NVIDIA_API_KEY"],
    base_url="https://integrate.api.nvidia.com/v1",
)
```

<!-- RESEARCH.md Pattern 3's exact repair-loop shape (adapt, do not reinvent) -->
```python
structured_llm = llm.with_structured_output(Plan, method="function_calling", include_raw=True)
result = structured_llm.invoke(prompt_messages)
# result = {"raw": AIMessage(...), "parsed": Plan(...) | None, "parsing_error": Exception | None}
```

From backend/app/graph/build.py (after Plan 03, current state before this plan):
```python
builder.add_node("stub_plan", stub_plan)
...
builder.add_edge("read_docs_greenfield", "stub_plan")
builder.add_edge("ingest_brownfield_stub", "stub_plan")
builder.add_edge("stub_plan", "human_review")
```
This plan removes `stub_plan` from the graph and substitutes `generate_plan`
in the same position (same incoming/outgoing edges), then deletes
`stub_plan.py`'s registration (file itself can remain unimported/removed —
Claude's discretion, but it must no longer be wired into `build_graph()`).

From frontend/src/lib/runClient.ts (current `Task` interface, already has
`skill_tag: string | null` — no interface change needed there, only RunPage
rendering needs to surface it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Skill taxonomy constant + Task.skill_tag taxonomy enforcement</name>
  <files>backend/app/models/skills.py, backend/app/models/plan.py</files>
  <read_first>backend/app/models/plan.py, .planning/phases/02-config-team-greenfield-planning/02-CONTEXT.md (D-10), .planning/phases/02-config-team-greenfield-planning/02-RESEARCH.md (Pitfall 4)</read_first>
  <action>
  Create `backend/app/models/skills.py` with `SKILL_TAXONOMY: list[str] = [
  "Frontend", "Backend", "Database", "DevOps", "Testing", "API-Design",
  "Auth", "Infra", "Mobile", "Data-Engineering", "ML", "Security",
  "Documentation", "UX-Design", "Integration"]` (15 items, within the D-10
  ~12-20 range) as the single canonical constant, importable by both
  `graph/nodes/generate_plan.py` and any future Phase 3 API/model consumers —
  do not duplicate this list anywhere else.

  In `backend/app/models/plan.py`, change `Task.skill_tag` from `str | None =
  None` to `skill_tag: Literal[tuple(SKILL_TAXONOMY)] | None = None` — Pydantic
  2.13 supports `Literal` built from a tuple constructed at class-definition
  time; if `Literal[tuple(...)]` syntax does not work directly (Python's
  `Literal` requires literal values known at type-check time, not a runtime
  tuple expansion in all cases), instead build the Literal type dynamically
  via `Literal.__getitem__(tuple(SKILL_TAXONOMY))` or fall back to Pitfall 4's
  alternative (b): keep `skill_tag: str | None = None` on the shared model
  (avoiding invasiveness to `push_to_ado.py`/frontend consumers) and add a
  standalone `validate_skill_tags(plan: Plan) -> None` function in
  `skills.py` that raises `ValueError` listing every task whose `skill_tag`
  is not in `SKILL_TAXONOMY` — call this from `services/llm.py`'s repair loop
  (Task 1) as an explicit post-parse validation step, exactly like a schema
  validation failure. Pick whichever approach actually type-checks/imports
  cleanly; document the choice in the task's completion notes. Either
  satisfies PLAN-02 as long as a taxonomy violation triggers the same
  repair-retry path as a malformed-JSON failure.
  </action>
  <acceptance_criteria>
  - `grep -c "SKILL_TAXONOMY" backend/app/models/skills.py` is greater than or equal to 1, and the list has between 12 and 20 string items
  - `cd backend && .venv/bin/python -c "from app.models.skills import SKILL_TAXONOMY; from app.models.plan import Task; print(len(SKILL_TAXONOMY))"` runs with no ImportError
  - A task constructed with a `skill_tag` value not in `SKILL_TAXONOMY` is either rejected by Pydantic at construction time (`Literal` approach) or caught by `validate_skill_tags` raising `ValueError` (approach (b)) — whichever was chosen, one of these two must be demonstrably true
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: LLM service — ChatOpenAI factory + validate-then-repair generation loop</name>
  <files>backend/app/services/llm.py, backend/tests/test_generate_plan.py, backend/requirements.txt, backend/.env.example</files>
  <read_first>.planning/phases/02-config-team-greenfield-planning/02-RESEARCH.md (Pattern 3, Pitfall 3, Pitfall 4, Assumptions Log A2), .planning/research/STACK.md (GLM via NVIDIA NIM — Exact Wiring)</read_first>
  <behavior>
  - Test 1: Given a mocked `structured_llm.invoke` that returns
    `{"raw": ..., "parsed": <valid Plan instance with all skill_tags in
    taxonomy>, "parsing_error": None}` on the first call,
    `generate_plan_with_repair` returns that `Plan` immediately (no retry
    call made).
  - Test 2: Given a mocked `invoke` that returns `parsing_error=<some
    exception>` on attempt 1 and a valid result on attempt 2,
    `generate_plan_with_repair` retries exactly once and returns the attempt-2
    `Plan`.
  - Test 3: Given a mocked `invoke` that returns a `Plan` with one task's
    `skill_tag` outside `SKILL_TAXONOMY` on attempt 1 and a fully valid `Plan`
    on attempt 2, the function detects the taxonomy violation (via whichever
    approach Task 0 chose) and retries, returning the attempt-2 result.
  - Test 4: Given a mocked `invoke` that always returns `parsing_error` set
    across `max_attempts` (default 3) calls, `generate_plan_with_repair`
    raises `RuntimeError` after exactly `max_attempts` calls (assert the mock
    was called exactly 3 times, not more, not fewer).
  - Test 5: Every task in a successfully returned `Plan` has
    `suggested_assignee == ""` — the prompt-building function
    (`build_plan_prompt`) must instruct the LLM not to guess assignees; add a
    defensive post-parse step in `generate_plan_with_repair` that force-blanks
    `suggested_assignee` on every task regardless of what the LLM returned,
    since D-14 requires this boundary to be clean even if the LLM ignores the
    instruction.
  </behavior>
  <action>
  Add `langchain-openai==1.3.4` to `backend/requirements.txt` (verify current
  top version via `pip index versions langchain-openai` first — RESEARCH.md
  confirmed this on 2026-07-10). Add `NVIDIA_API_KEY=` and
  `NVIDIA_CHAT_MODEL=z-ai/glm-5.2` entries to `backend/.env.example` (per
  STACK.md's "CRITICAL FLAG" — this is a placeholder value in `.env.example`
  only; do not hardcode it in `llm.py`).

  Create `backend/app/services/llm.py`:
  - `def build_chat_llm() -> ChatOpenAI` — constructs `ChatOpenAI(model=
    os.environ["NVIDIA_CHAT_MODEL"], api_key=os.environ["NVIDIA_API_KEY"],
    base_url="https://integrate.api.nvidia.com/v1")`, reading both env vars
    fresh (no import-time env reads, matching `ado_client.py`'s established
    pattern).
  - `def build_plan_prompt(docs_text: str, skill_taxonomy: list[str]) -> list`
    — builds the message list (a system + human message, or a single human
    message — Claude's discretion) instructing the LLM to: produce 2-5 epics
    with 2-6 tasks each (D-13), tag every task's `skill_tag` with EXACTLY one
    value from `skill_taxonomy` (enumerate the taxonomy list explicitly in
    the prompt text), estimate every task's `estimate_hours` as a positive
    float, leave `suggested_assignee` as an empty string for every task
    (D-14), and ground the plan in the provided `docs_text` (include it in a
    clearly delimited section, e.g. `"--- PROJECT DOCS ---\n{docs_text}\n---
    END PROJECT DOCS ---"`, treating it as data per the prompt-injection
    mitigation noted in Plan 03's threat model).
  - `def build_repair_prompt(original_prompt: list, error: str) -> list` —
    appends a message describing the previous failure (`error` string) and
    instructing the LLM to correct it, re-including the original prompt
    content.
  - `def generate_plan_with_repair(llm, docs_text: str, skill_taxonomy:
    list[str], max_attempts: int = 3) -> Plan` — implements RESEARCH.md
    Pattern 3's loop exactly: `structured_llm =
    llm.with_structured_output(Plan, method="function_calling",
    include_raw=True)`; loop `max_attempts` times, first attempt uses
    `build_plan_prompt`, subsequent attempts use `build_repair_prompt` with
    the last error; on `parsing_error is not None`, record the error and
    continue; on success, re-validate via `Plan.model_validate(result
    ["parsed"])` (defense-in-depth per Pitfall 3), check taxonomy compliance
    (Task 0's chosen mechanism), force-blank every task's
    `suggested_assignee` to `""` (D-14 defensive enforcement, Test 5), and
    return the `Plan`; after exhausting `max_attempts`, raise `RuntimeError`
    with the last error message included (PLAN-04's "fail loudly" requirement
    — never return a partially-broken `Plan`). Set a generous `max_tokens`
    consideration: if `ChatOpenAI` accepts a `max_tokens` kwarg in
    `build_chat_llm`, set it to `8192` per Pitfall 3's truncation-avoidance
    guidance.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_generate_plan.py -x</automated>
  </verify>
  <done>generate_plan_with_repair returns a valid Plan on first-try success, retries on parse/taxonomy failure up to max_attempts, raises RuntimeError after exhausting retries, and always returns tasks with suggested_assignee="".</done>
</task>

<task type="auto">
  <name>Task 2: Wire generate_plan node into the graph, replacing stub_plan; surface skill_tag in the frontend</name>
  <files>backend/app/graph/nodes/generate_plan.py, backend/app/graph/build.py, backend/app/graph/nodes/stub_plan.py, frontend/src/pages/RunPage.tsx</files>
  <read_first>backend/app/graph/nodes/stub_plan.py, backend/app/graph/build.py, backend/app/graph/nodes/read_docs_greenfield.py, frontend/src/pages/RunPage.tsx</read_first>
  <action>
  Create `backend/app/graph/nodes/generate_plan.py`: `async def
  generate_plan(state: RunState) -> dict`. First check
  `state.get("blocked_reason")` — if set (non-None, from either the
  no-docs-greenfield case or the brownfield placeholder, both from Plan 03),
  short-circuit immediately and return `{"plan": Plan(epics=[])}` (an empty
  but valid `Plan` instance, per RESEARCH.md's explicit guidance that
  `generate_plan` must check this flag first rather than attempting real
  planning from empty/placeholder context) — do NOT call the LLM in this
  branch. Otherwise, call `build_chat_llm()`, then
  `generate_plan_with_repair(llm, state["docs_text"], SKILL_TAXONOMY)` and
  return `{"plan": result}`. Let `RuntimeError` from
  `generate_plan_with_repair` propagate uncaught (LangGraph surfaces node
  exceptions to the run's status naturally; do not swallow it here — PLAN-04
  requires failing loudly).

  In `backend/app/graph/build.py`: remove the `stub_plan` import and its
  `builder.add_node("stub_plan", stub_plan)` registration; import and
  register `generate_plan` in its place using the exact same node name
  position in the edge list — replace both
  `builder.add_edge("read_docs_greenfield", "stub_plan")` and
  `builder.add_edge("ingest_brownfield_stub", "stub_plan")` with the same two
  lines pointing at `"generate_plan"`, and
  `builder.add_edge("stub_plan", "human_review")` becomes
  `builder.add_edge("generate_plan", "human_review")`. Delete
  `backend/app/graph/nodes/stub_plan.py` (no longer referenced anywhere; a
  dangling unimported stub file would be dead code inconsistent with
  CLAUDE.md's scope discipline).

  In `frontend/src/pages/RunPage.tsx`, update the plan-rendering block to
  also show `task.skill_tag` (e.g. append `— skill: {task.skill_tag ??
  "none"}` after the existing `estimate: {task.estimate_hours}h` text) so the
  lead can see PLAN-02's output land in the UI. No new interface change
  needed in `runClient.ts` (`skill_tag: string | null` already exists there
  from Phase 1).
  </action>
  <acceptance_criteria>
  - `grep -c "stub_plan" backend/app/graph/build.py` equals 0
  - `grep -c "generate_plan" backend/app/graph/build.py` is greater than or equal to 2 (import + node registration, at minimum)
  - `test -f backend/app/graph/nodes/stub_plan.py` fails (file removed)
  - `grep -n "skill_tag" frontend/src/pages/RunPage.tsx` returns a match
  - `cd backend && pytest -x` (full suite) passes with generate_plan wired in and stub_plan gone
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| docs_text -> LLM prompt | Repo-controlled content flows into the plan-generation prompt; treated as delimited data, not instructions (Plan 03's T-02-06, now actually mitigated here via prompt structure) |
| GLM/NIM response -> Plan schema | Untrusted model output must pass Pydantic validation + taxonomy check before being trusted as a `Plan` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | Malicious repo docs attempting prompt injection to alter generated plan content | mitigate | `build_plan_prompt` wraps `docs_text` in explicit `--- PROJECT DOCS ---` / `--- END PROJECT DOCS ---` delimiters and instructs the LLM to treat it as reference material, not commands; low-severity residual risk accepted for a local single-lead MVP |
| T-02-10 | Tampering | Malformed/adversarial LLM tool-call output (corroborated by an open NVIDIA NIM bug report) | mitigate | validate-then-repair retry loop (generate_plan_with_repair) with bounded attempts and a hard failure (RuntimeError) rather than accepting broken output |
| T-02-11 | Information Disclosure | NVIDIA_API_KEY | mitigate | Read fresh from `os.environ` inside `build_chat_llm`, never logged or echoed to the frontend |
</threat_model>

<verification>
- `cd backend && pytest -x` (full suite across all four Phase 2 plans) passes
- Manual (requires a valid `NVIDIA_API_KEY`/`NVIDIA_CHAT_MODEL` and a real greenfield `GITHUB_REPO` with docs): start a run, confirm the generated plan has 2-5 epics, each task has a `skill_tag` from the taxonomy and a positive `estimate_hours`, and every `suggested_assignee` is `""`
</verification>

<success_criteria>
- PLAN-01 satisfied: real epic->task plan generated, bounded 2-5 epics / 2-6 tasks each
- PLAN-02 satisfied: every task's skill_tag is constrained to the fixed taxonomy
- PLAN-03 satisfied: every task has an hours estimate
- PLAN-04 satisfied: malformed/invalid structured output is caught and retried automatically; exhausting retries fails loudly, never emits broken plan data
- D-14 satisfied: suggested_assignee is "" on every generated task
</success_criteria>

<output>
Create `.planning/phases/02-config-team-greenfield-planning/02-04-SUMMARY.md` when done
</output>
